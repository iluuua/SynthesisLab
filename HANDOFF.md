# SynthesisLab — HANDOFF (контекст для нового чата)

Факт-лист для продолжения работы. Всё по делу, без воды.

## 1. Задача
- Сделать сайт под ключ для **СинтезЛаб** (командный/стратегический коучинг). Дизайн → разработка → деплой.
- Дедлайн: **вечер четверга**. Награда исполнителю **$500 USDT** (платит @AngelInvestor2030).
- Текущий сайт (контекст, Tilda): **https://academysbd.ru/**.
- Пожелания заказчика: минималистично, просто, читаемо, без ИИ-слопа, «без кубернетесов», не перегружать.

## 2. Иерархия (как работаем)
- **CEO/супервайзер = Claude**: планирование, спека, контент, сборка-ассембли, ревью, фиксы.
- **Исполнитель кода = MiMo** (`mimo-v2.5-pro` через GitLawB OpenGateway).
  - Endpoint: `https://opengateway.gitlawb.com/v1/chat/completions`, model `mimo-v2.5-pro` (реально `xiaomi/mimo-v2.5-pro-20260422`).
  - Ключ `OPENGATEWAY_API_KEY` в `system/.env` и `~/.hermes/.env` (не печатать/не коммитить).
  - **Особенности (важно):** reasoning-модель; **обязательно streaming** (`"stream":true`) иначе connection reset на длинных ответах; провайдер режет вывод ~**5 файлов/вызов** (`finish_reason: length`) → генерить мелкими батчами по 4–5 файлов; иногда закрывает заголовок файла `---` вместо `===` и лепит фронтматтер `.astro`. Парсер должен быть line-based и лояльный к концовке заголовка; `.ts` файлы НЕ должны получать `---`.

## 3. Бриф (подтверждено клиентом — Татьяна/Екатерина)
- **Цель сайта:** вести на **бесплатную диагностику** через **форму** + сайт-визитка.
- **Клиент №1:** собственник, HR. Тон на «вы», русский.
- **Бренд:** СинтезЛаб. Главный CTA везде: **«Получить диагностику»** → форма.
- **4 услуги:** Командные сессии · Стратегические сессии · Корпоративная медиация · Индивидуальный коучинг лидеров.
- **Услуги:** одна страница + 4 детальные секции с якорями из карточек; контент в `src/data/services.ts` (позже легко вынести в `/services/[slug]`). Тексты услуг — придут позже.
- **Цены:** на главной НЕ показываем; «от X ₽» + «смета после диагностики» — в детальной секции услуги. (Совпало с идеей клиента + best practice.)
- **Форма (поля):** Имя · Компания · Роль (Собственник/HR/Руководитель/Другое) · Контакт (телефон или Telegram) · Коротко о запросе.
- **TG-бот у них есть** (нужны token + chat_id для боевой формы).
- **Т-Банк разрешил отзыв/кейс под своим именем.** Премия НФПМК 2024 — их кейс с Т-Банком.
- **Команда — 4 человека:** Татьяна Петрова, Екатерина Деревяшкина, Александра Теплицкая, Анастасия Львова. (Убраны: Блинова, Алонцева, Барабанова.) Био Александры/Анастасии — пока НЕТ.
- **Домен:** хотят сменить, список свободных НЕ дошёл (нужно переспросить). Пока деплой на домен Amvera.
- **«Наш подход» — финальные тексты (короткие, зачёркнутое в исходнике убрано):** ромб «Ориентированы на честность и прозрачность.» / сердце-в-ладонях «Верим в потенциал команды.» / фигуры «Создаём решения для устойчивого развития команд и бизнеса.» / шестерёнка «Работаем с системой в целом.»
- **Видео с подкаста** (Татьяна, Екатерина, и вместе) — есть, неотмонтированы; опциональный видео-блок на потом.

## 4. Бизнес-факты (для контента)
- Сооснователи: **Татьяна Петрова** (Executive-коуч PCC ICF, фасилитатор, член экспертного совета Центра развития лидеров и команд, спикер), **Екатерина Деревяшкина** (Executive/командный коуч, бизнес-психолог, ментор по внедрению командного коучинга, победитель премии НФПМК 2024, кейс Т-Банк). TG: Катя @teamcoach007 / @psycoach_katerina, Таня @tatiana_doitnow.
- Метрики: **800+ сессий · 300+ команд · 250+ лидеров · 12 лет**.
- Цены-якоря (с текущего сайта, уточнить): командные от 180 000 ₽; стратегические от 160 000 ₽; медиация от 60 000 ₽; индив. коучинг от 15 000 ₽.
- Кейсы (в `cases.ts`): Т-Банк/НФПМК-2024 (featured); интеграция 2 команд на единую IT-платформу; поддержка команды/лидера в изменениях (80 чел / управление 25 000 / 5 мес); медиация собственников (12×1.5ч).
- Контакты текущие: тел `+7 918 312 07 20`, почта `kt912@mail.ru` (личная mail.ru — заменить на брендовую). В лого опечатка: «системное развит**е** бизнеса» → должно «развит**ие**».

## 5. Репозиторий и стек
- Путь: **`/Users/ilua/Documents/ilua-system/projects/SynthesisLab`** (git инициализирован, кроме Initial commit НЕ коммичено; коммитить только по запросу пользователя).
- Стек: **Astro v4 + Tailwind v3 + TypeScript**, `output: 'hybrid'`, адаптер **@astrojs/node** (standalone) — страницы пререндерятся, только `/api/lead` серверный. Node v22. Шрифт **Inter** (@fontsource).
- **Палитра (Tailwind):** ink `#3D5377`, periwinkle `#9DB0D6`, surface `#F4F6FA`, accent `#E8924A` (только primary CTA), muted `#6B7689`, line `#E4E8F0`. `max-w-content` = 1120px.
- Команды: `npm install`; `npm run build`; превью (node): `HOST=127.0.0.1 PORT=4321 node ./dist/server/entry.mjs` → http://127.0.0.1:4321/ ; dev: `npm run dev`.
- Деплой (наш стандарт, `system/registry/deploy_targets.yaml`): **Amvera** `amvera_webhook_branch` (push в prod-ветку → сборка → запуск). Node-приложение: запуск `node ./dist/server/entry.mjs`. Нужны Dockerfile/amvera.yml (ещё НЕ сделаны).

## 6. Структура (что есть на диске)
- `src/data/`: `content.ts` (site, contacts, nav, hero, reinforce, diagnostic, method, approach, metrics, clients, faq), `services.ts` (4 услуги + priceNote), `cases.ts` (4 кейса), `team.ts` (4 чел + фото).
- `src/components/`: `ui/` (Container, Section, Button, Icon) + Header, Hero, Reinforce, Formats, FormatCard, DiagnosticCTA, ServiceDetail, Method, Cases, CaseCard, Clients, Reviews, Approach, Team, TeamCard, Faq, LeadForm, Footer.
- `src/pages/`: `index.astro` (ассембли в утверждённом порядке), `privacy.astro`, `api/lead.ts` (POST, honeypot, валидация, Telegram через env, иначе mock-лог).
- `src/assets/`: `logo/` (logo-horizontal.png, logo-vertical.png), `photos/` (tatiana.jpg, ekaterina.jpg, founders.jpg — downscaled 1333×2000; founders пока не используется).
- `materials_raw/`: исходные лого (PNG + `СИНТЕЗЛАБ.pdf` вектор), оригиналы фото (Татьяна.jpg, Екатерина.jpg, Татьяна_и_Екатерина.jpg ~4480×6720), брендбук = только лого.
- `research/`: скриншоты текущего сайта + превью нового (`preview-desktop.png`).
- Спека: **`SPEC.md`** (канон). Вопросы клиенту: `../../agent-vault/10-projects/ekaterina-site/questions-v2.md`. План: тот же vault-каталог.

## 7. Текущее состояние (DONE)
- Сайт **собирается чисто** (`npm run build` зелёный), все секции рендерятся, форма `/api/lead` работает в mock-режиме (валид → `{ok:true}` + лог; honeypot → ok; без полей → 400).
- Изображения авто-оптимизируются в webp (лого 3КБ, портреты 9–12КБ).
- Превью видели — целостный премиум-минимал лендинг, палитра от лого, оранжевый только на CTA.

## 8. Открытые задачи (TODO)
1. **Дизайн-полиш:** hero посильнее (presence/тип-шкала), ритм/отступы, мобильная вёрстка, плотность блока «Что входит в каждый формат», фавикон + OG-картинка, Lighthouse/a11y.
2. **Боевая форма:** получить от клиента **TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID** → в `.env` (есть `.env.example`/`.gitignore` исключает `.env`).
3. **Контент-слоты:** тексты 4 услуг; био+фото Александры и Анастасии; фото с сессий; логотипы клиентов (можно забрать SVG с tildacdn текущего сайта); реальные отзывы (Т-Банк — первым).
4. **Домен:** переспросить список; подключить на Amvera + SSL; брендовая почта вместо kt912@mail.ru.
5. **Деплой:** Dockerfile/amvera.yml под Node-standalone, prod-ветка, healthcheck.
6. **Подтвердить:** акцентный цвет (по умолчанию оранжевый `#E8924A`); исправление опечатки в лого (нужен редактируемый SVG).

## 9. Грабли/уроки
- MiMo: streaming обязателен; батчи ≤5 файлов; парсить line-based, лояльно к `===FILE: path---`/`===`; снимать `---` с `.ts`.
- Astro hybrid: для API-роута нужен adapter (поставлен @astrojs/node); `export const prerender = false` в `api/lead.ts`.
- gstack browse: перед `screenshot` обязательно успешный `goto` (иначе пустой ~4КБ PNG); сервер поднимать отдельно (node entry), затем `goto`.
- Astro `output:'static'` НЕ умеет серверные эндпоинты — поэтому hybrid+node.
